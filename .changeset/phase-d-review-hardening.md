---
'@foldstryx/foldkit': patch
---

Harden Phase D review findings: use the exported `titleId`/`descriptionId` helpers for Dialog labelling and clear `aria-describedby` when no description is present, add an opt-in Escape-keydown close path with Scene coverage, and add backdrop-click dismissal Scene coverage.
