---
'@foldstryx/styles': patch
---

Match the Astryx fill-mode shell scroll contract: bound the sidebar demo shell to `100dvh`, make the rail and inset flex children with `min-height: 0`, and give the nav and main regions independent vertical scroll containers so the rail and inset header stay pinned while content scrolls.
